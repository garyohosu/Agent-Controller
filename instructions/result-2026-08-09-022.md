# Result: instruction-2026-08-09-015

判定: `V1_RELEASE_READY_WITH_KNOWN_LIMITATIONS`

## 実施内容

- `pyproject.toml` の package metadata を確認し、version を `1.0.0` に更新した。
- `uv.lock` を metadata と同期した。
- 正式CLI入口 `agent-controller` を確認した。
- README に第三者向けの install、Main Graph、CLI、既知制限、release boundary を追加した。
- `RELEASE_CHECKLIST.md` を追加した。
- 過去の instruction/result は証拠として保持し、現行実装の判定には使い回していない。

## CLI実動確認

以下を SQLite の一時 run に対して実行した。

- `uv run agent-controller --help`
- `status`
- `questions`
- `show`
- `answer QUESTION_ID "answer"`
- `answer-batch answers.json`

`answer` と `answer-batch` は HUMAN_REQUIRED の run で質問を ANSWERED に更新し、SQLite と QandA の同期を確認した。

## テスト

通常の外部AI非依存テスト:

```text
272 passed, 4 skipped in 56.04s
```

fresh clone 相当の別ディレクトリでも `uv sync`、CLI `--help`、pytest を実行し、次を確認した。

```text
272 passed, 4 skipped in 45.37s
```

`git diff --check` も通過した。Git checkpoint / rollback、CompleteGate、fallback、Q&A、Main Graph scripted E2E の既存テストも上記全体テストに含まれる。

## 実AI E2E

Codex 基準の Main Graph 実AI E2E は前工程の新規 scratch workspace で COMPLETE 到達を確認済みであり、本変更はその経路を変更していない。Claude は短い read-only Reviewer contract では連続 PASS を確認し、長い通常 Reviewer payload では設定 timeout に到達する既知制限を再確認している。Claude timeout 後の Codex fallback により COMPLETE へ到達する経路も確認済みである。

この Claude runtime 制限を成功扱いに偽装せず、v1.0 の blocker にはしないという instruction 015 の境界に従った。

## セキュリティと公開境界

- tracked files と作業ツリーを credential らしい `sk-`、GitHub token、Slack token、Bearer token のパターンで監査し、該当なし。
- 診断 JSONL は repository 外の一時診断領域に保存され、今回の commit 対象ではない。
- README に秘密情報を記録しない運用境界を反映した。
- v1.0.0 tag および GitHub Release は作成していない。

## 残る既知制限

長い Claude Reviewer payload は環境依存で worker timeout になる場合がある。短縮 read-only Reviewer と Codex fallback は機械判定経路を持つが、Claude の長いpayloadを全面的に安定化したとは主張しない。agy の実利用権限など過去結果に記録された環境依存制限も、未確認の成功として扱わない。

## 最終状態

この結果、v1.0公開「準備」は完了した。公開者による最終確認後にのみ tag/release を作成できる状態であり、今回の作業では tag/release を作成していない。
