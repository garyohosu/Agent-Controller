# Result 2026-08-09-018

## 最終判定

`ROOT_CAUSE_CONFIRMED_AND_FIXED`

## 原因と確度

前回の120秒はCodex CLI内部のtimeoutではなく、実行Harness/外側shellの120秒上限で発生していた。前回コードではMain GraphのREVIEWからQUESTIONが出たとき、QandAへ進めるDirector処理がなく、QUESTIONを保存した後に正常なQANDA分類・再開遷移を完了できない経路だった。これが外側120秒の間に完了せず、SQLiteにはOPEN questionが残った。

原因確度は `CONFIRMED`。根拠は、旧経路のSQLite時系列、修正前の停止位置、同じQ&Aだけを使った最小Probe、修正後の実AI E2E、Worker invocation診断ログの一致である。

## Timeout層

- 前回の120秒: 外側PowerShell/exec Harness。
- `CliWorker.timeout`: 前回Main Graph実行では既定900秒で、120秒を発生させた層ではない。
- 最小Codex Q&A: `timeout_seconds=120`, `elapsed_ms=16968`、exit 0、QUESTION。Worker timeoutではない。
- 修正後Main Graph: IMPLEMENT 43,720ms、REVIEW 30,813ms、外側120秒以内にCOMPLETE。

## 時系列と診断ログ

Workerごとに以下をJSONLへ記録するようにした。

`invocation_id`, `run_id`, `worker`, `role`, `state`, `phase`, `started_at`, `finished_at`, `elapsed_ms`, `controller_elapsed_ms`, `timeout_seconds`, `resolved_executable`, raw/signed exit code, timeout flag, prompt chars/bytes, artifact chars, question chars, stdout/stderr chars, result event。

QANDAのquestion保存、formal answer、provisional保存、HUMAN_REQUIRED保存にも `qanda_step` と `elapsed_ms` を記録する。診断JSONLはGit workspace外に置き、実行treeを汚さない。

修正後Main Graphの主要ログ:

| phase | worker | elapsed_ms | result |
|---|---|---:|---|
| IMPLEMENT/GENERATE | CODEX_CLI | 43,702 | DONE |
| REVIEW/REVIEW_LIGHT | CODEX_CLI | 30,813 | PASS |

## Q&A prompt内訳

同一質問の最小Probeでは、Codex/Claude/Grok/agyへ同じWorkerRequestを渡した。

- prompt: 3,877 chars / 3,877 UTF-8 bytes
- Controller-attached artifacts: 494 chars（SPEC.md + README.md）
- Q&A directive/question部分: 約1,568〜1,605 chars
- stdout: Codex QUESTION時459 chars、Codex provisional DONE時583 chars
- Codex stderr: 約5,019 chars（CLIのdiagnostic/tool logを含む）
- Q&Aに巨大なtransition historyや同一artifact全文の重複は確認されなかった

## 4AI比較

| worker | elapsed | exit | result | evidence |
|---|---:|---:|---|---|
| Codex | 12,093〜16,968ms | 0 | QUESTIONまたはLOW_RISK_REVERSIBLE DONE | structured JSONあり |
| Claude | 60,093ms | -1 | WORKER_ERROR timeout | stdout 0、stderr 19 chars |
| Grok | 30,453〜60,125ms | -1 | WORKER_ERROR timeout | stdout 0、stderr 19 chars |
| agy | 11,110〜13,483ms | 0 | WORKER_ERROR | headless command permission拒否、stdout 0 |

成功stdout内の429等をresource limitとは誤判定していない。agyのexit 0も成功扱いせず、JSONなしをWORKER_ERRORとした。

## 最小再現

前回OPEN questionの内容、SPEC.md、README.md、QANDA phase、DIRECTOR roleを固定した最小WorkerRequestを実行した。Codexは複数回とも約12〜17秒で構造化結果を返し、Q&A単体では120秒timeoutを再現しなかった。したがってprompt量やCodex QANDA自体が主因ではない。

## 修正内容

- Worker invocation診断を拡張し、サイズ・時刻・exit・timeout・実行ファイル・結果を記録。
- Worker dispatch後にController側elapsedとcandidate indexをJSONLへ記録。
- QANDA内部の保存・描画step timingを記録。
- Main Graph REVIEW QUESTIONをDirector/QANDAへ送り、LOW_RISK_REVERSIBLEだけ暫定判断として再開する経路を追加済み。
- Claude/Grok/agyの診断にも共通サイズ・invocation metadataを追加。
- timeout値を解決策として延長していない。

## 検証結果

- `uv run pytest -q`: `270 passed, 4 skipped`
- 修正後Codex-only Main Graph E2E: `IDLE → DESIGN → IMPLEMENT → TEST → REVIEW → DOC_SYNC → COMPLETE`
- TESTは実コマンドを実行しexit 0。
- REVIEWは実Codex read-only Worker。
- Q&A 0件でCompleteGate通過。
- SQLite/QandA本文の手編集なし。

## Git

最終commit、push、`HEAD == origin/main`、clean確認はcommit後に記録する。
