# 実施結果 013

対象: `instructions/instruction-2026-08-09-006.md` 実AI Main Graph E2E

## 最終判定

実AI E2Eはscratch Git workspaceでMain Graph入口から開始したが、COMPLETEには到達しなかった。最終状態は `HUMAN_REQUIRED`。CompleteGateの迂回、SQLiteの手編集、ArtifactStatusの強制変更、入力SPECの改変は行っていない。

## scratch workspace

```text
C:\Users\garyo\AppData\Local\Temp\agent-controller-live-e2e-001
```

初期commitは `a82a41e15aab8b52e06081c7b66651f162e7b861`、local bare repositoryをupstreamに使用した。初期SPECは `greet NAME` CLIの正常系/空引数終了コード2/pytestカバレッジを定義し、実行前にcommit + pushした。

正式なAgent-Controller repoのtracked fileは変更していない。

## Worker / quality mode

- DESIGN Implementer: Codex CLI
- DESIGN Reviewer: Claude Code
- IMPLEMENT: Codex CLI
- REVIEW: Claude Codeを試行後、Claude停止を受けてCodex単独degraded modeを試行
- quality_mode: `SINGLE_AGENT`（Claude Codeが4294967295で終了し、CodexのReviewer fallbackも同じ終了コードで停止）

## 実測したトップレベル遷移

設計完了までは次の経路を通った。

```text
IDLE → DESIGN → IMPLEMENT → TEST
```

TESTの実行後、1回目は `exit_code=120` で機械的に `TEST → IMPLEMENT`。再実行では `exit_code=0` となり `TEST → REVIEW` まで進んだ。その後、実AI Reviewerが `CLAUDE_CODE exited with 4294967295` となり、既存経路で `REVIEW → HUMAN_REQUIRED`。Codex Reviewer fallbackも同じ終了コードとなり、`IMPLEMENT → HUMAN_REQUIRED` が記録された。

`CompleteGate` は `DOC_SYNC + PASS` に到達していないため、実行されるCOMPLETE遷移は発生していない。COMPLETEへ強制遷移する操作はしていない。

## 代表的なDocumentStage遷移

```text
DESIGN/SPEC/REVIEW_DEEP → DONE → DESIGN/USECASE/GENERATE
DESIGN/USECASE/GENERATE → DONE → DESIGN/USECASE/REVIEW_DEEP
DESIGN/USECASE/REVIEW_DEEP → PASS → DESIGN/SEQUENCE/GENERATE
DESIGN/SEQUENCE/GENERATE → DONE → DESIGN/SEQUENCE/REVIEW_LIGHT
DESIGN/SEQUENCE/REVIEW_LIGHT → PASS → DESIGN/CLASS/GENERATE
DESIGN/CLASS/GENERATE → DONE → DESIGN/CLASS/REVIEW_LIGHT
DESIGN/CLASS/REVIEW_LIGHT → PASS → DESIGN/TESTCASE/GENERATE
DESIGN/TESTCASE/GENERATE → DONE → DESIGN/TESTCASE/REVIEW_LIGHT
DESIGN/TESTCASE/REVIEW_LIGHT → PASS → IMPLEMENT
```

DocumentStageのPASSが単独でIMPLEMENTへ飛ぶことはなく、全設計stage完了後に `run_design()` がトップレベルPASSを返した。

## 実AI成果物

Codex/Claudeの実Workerにより、USECASE.md、SEQUENCE.md、CLASS.md、TESTCASE.md、greet package、testsが生成された。既存SPEC.mdは変更されていない。各Worker成功時には§17-14のController commitが作られた。

TEST commandは実際に次を実行した。

```text
python -m pytest -q
```

独立した同一commandの診断実行は `2 passed in 0.27s` だった。一方、Graph実行中には自然に `exit_code=120` が発生し、その後再実行で `exit_code=0` を記録した。TEST結果の自由文による補正はしていない。

## 発生した問題

1. 最初の実AI起動は外部command上限124秒で停止した。SQLiteには `DESIGN/USECASE/REVIEW_DEEP` が保存され、USECASE commitも残ったため、同じrunをその位置から再開した。
2. Graph中のTESTは一度終了コード120となった。手動の同一pytestはPASSだったため、原因を断定せず、Controllerはその終了コードをFAILとして扱った。
3. Claude Codeは `4294967295` で終了し、Codex単独Reviewer fallbackも同じ終了コードで終了した。Worker異常を `WORKER_ERROR` としてSQLite transitionに記録し、HUMAN_REQUIREDへ停止した。

自然発生した異常を隠すための入力変更や、期待Eventを出すための再試行はしていない。実AI E2E完走は未達のため、README/hikitsugiには「実AIでCOMPLETE済み」とは記載していない。

## 通常テスト

```text
257 passed in 35.14s
```

通常pytestは外部AI CLI / network非依存のまま維持した。

## 更新ファイル

- `README.md`
- `hikitsugi.md`
- `instructions/result-2026-08-09-013.md`

実装コードの追加修正は、今回の実AI停止原因を再現可能な形で確定できなかったため行っていない。

## Git

- 指示書pull commit: `32b2e57925e5afc56fd111ae330fd58fe420ca20`
- 本結果書を含むcommit: push後に確認する。
- 本体repoのpush / HEAD == origin/main / clean確認を完了させる。
- scratchは実行途中のSQLiteと未追跡pycacheを含むため、実AI証跡として保持する。正式repoのclean条件とは分離している。
