# Result 2026-08-09-016

## 結論

§17-16 相当の質問分類・暫定判断・回答バッチを実装した。通常運用では未承認の暫定判断を CompleteGate が block し、scratch 運用では `allow_provisional=True` を明示した場合だけ通過できる。

## 実装

- `QuestionStatus.PROVISIONAL` と分類、risk、可逆性、影響Artifact、暫定回答、Human確認要否をSQLiteへ保存。
- QANDA は `LOW_RISK_REVERSIBLE` の明示的な構造化結果だけを暫定判断として保存し、正式回答とは区別する。
- IMPLEMENT の QUESTION は Director の QANDA 分類へ送る。低リスク可逆だけ再開し、それ以外は CANNOT_ANSWER / HUMAN_REQUIRED 経路を維持する。
- `answer-batch` CLI と `answer_batch()` を追加し、複数の HUMAN_REQUIRED を一括回答できるようにした。
- `UNAPPROVED_PROVISIONAL_DECISION` を CompleteGate に追加した。自由文やQandA.md本文は判定根拠にしていない。

## 検証

- 通常テスト: `266 passed, 4 skipped`
- Codex-only 実AI Main Graph E2E: scratch Git workspaceで完走。
- 実行経路: `IDLE → DESIGN → IMPLEMENT → TEST → REVIEW → DOC_SYNC → COMPLETE`
- TESTは `python -c "assert True"` を実行し終了コード0を機械判定。
- REVIEWはCodex CLIをread-only Reviewerとして実行。
- CompleteGateはSQLiteのArtifact状態、TEST/REVIEW、README、Git clean/commit/pushを確認。
- 最終E2EはQ&A 0件でCOMPLETE。途中の質問は発生しなかったため、別途ユニットテストでPROVISIONAL保存と通常Gate blockを確認した。

## 制限事項と証拠

初回実AI E2Eは120秒でタイムアウトし、IMPLEMENTで同種QUESTIONが4件OPENになった。これはIMPLEMENT直結QUESTIONをDirector分類へ送る配線不足を示したため、隠さず記録し、その配線を修正した。修正後の再実行でCOMPLETEへ到達した。

変更後の作業tree、commit、push、HEADとorigin/mainの一致は最終確認で記録する。
