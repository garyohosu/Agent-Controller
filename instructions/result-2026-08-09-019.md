# instruction-2026-08-09-012 実施結果

## 最終判定

`RESULT_PRODUCED_WITH_LIMITATIONS`

Codex単独の基準E2EはCOMPLETEまで到達済みで、残PARTIALのうちController実装で閉じられるものは修正した。一方、同一runでClaudeを含む2AI受入は、Claudeのread-only timeout後にCodexへfallbackしたが、Codex Reviewerが同じQ&Aを再質問し続け、COMPLETEには到達しなかった。未達をCOMPLETE扱いにはしていない。

## 要求単位の再判定

| 項目 | 判定 | 根拠 |
|---|---|---|
| §6 Impact Analysis | `INTENTIONAL_SIMPLIFICATION` | `ImpactAnalyzer`、upstream change、artifact freshnessは実装済み。影響グラフを独立サービスとして持たず、Controller内の設計ループで評価する構成。 |
| §7 Director directive | `MVP_REQUIRED_AND_FIXED` | Reviewerの構造化findingを次のIMPLEMENT directiveへ渡す。Q&AはDirector phaseへ送る。 |
| §8 REVIEW→IMPLEMENT loop | `MVP_REQUIRED_AND_FIXED` | scripted統合テストでFAIL/finding→IMPLEMENT→TEST→REVIEWを確認。Git checkpoint rollbackも経由する。 |
| §14 DOC_SYNC / README | `MVP_REQUIRED_AND_FIXED` | `readme_sync` callbackの構造化結果 `LATEST` / `NOT_REQUIRED`、README artifact、Git push、CompleteGateを接続。AI自由文は判定に使わない。 |
| §17 Main Graph実AI | `MVP_REQUIRED_AND_FIXED`（Codex基準）/ `POST_MVP`（4AI全成功） | Codex基準E2EはCOMPLETE。複数AIの環境差は後述の既知制限。 |
| Role routing | `MVP_REQUIRED_AND_FIXED` | `role_router_from_adapters()` と既定優先順位を追加し、テストで固定。 |
| timeout診断 | `MVP_REQUIRED_AND_FIXED` | Worker invocationごとにJSONLを記録。Worker timeoutと外側Harness timeoutを別フィールド・別事象として記録。 |

## Role routing / 4AI受入状況

| Role | 優先順 | 実行状況 |
|---|---|---|
| IMPLEMENTER | Codex → Claude | Codex write-enabled経路を基準。Claude write経路は環境検証待ち。 |
| REVIEWER | Claude → Codex → Grok | Claudeはread-only timeoutを観測。Codex fallbackは起動し、Q&Aを返した。Grokはprobeでtimeout。 |
| DIRECTOR | Codex → Claude → Grok | Codex DirectorのQ&A回答を確認。 |
| ANSWERER | Codex → Claude → Grok | Codexを基準。Claude/Grokは環境制約を記録。 |
| agy | 既定route外 | headless CLIのpermission errorで構造化結果を取得できず、接続を急がない。 |

既定routingは利用可能なadapterだけを選び、agyを暗黙に成功扱いしない。Grok/agyは当面read-only用途に限定する方針を維持する。

## 受入試験

### Acceptance A: Codex基準

既存のscratch E2E証拠を再確認した。

```text
IDLE → DESIGN → IMPLEMENT → TEST → REVIEW → DOC_SYNC → COMPLETE
final state=COMPLETE, status=COMPLETED
pytest=270 passed, 4 skipped
```

IMPLEMENT / REVIEW は実Codex CLI、TESTはscratch workspace内のpytest実行、DOC_SYNCは構造化README statusとGit処理、最後にCompleteGateを通過した。

### Acceptance B: 同一runで2AI

scratch: `C:\Users\garyo\AppData\Local\Temp\agent-controller-live-e2e-018`

1. IMPLEMENTER Codex: 43.6秒、exit 0、DONE。
2. REVIEWER Claude: 30秒、exit -1、timeout。`--permission-mode plan`, `Read,Glob,Grep` のread-only実行。
3. REVIEWER Codex fallback: 43.8秒、exit 0、QUESTION。
4. Director Codex: 22.7秒、DONE。Q&A answerを保存。
5. 同じReview QUESTIONが再発し、runはHUMAN_REQUIREDへ到達せず外側Harnessの上限で停止。

この試験では同一run内にCodexとClaudeの実AI invocationが存在し、Claude失敗後のfallback自体は自然発生した。しかしCOMPLETE未達のため、Acceptance Bは不合格とする。診断JSONLには各 invocation の `invocation_id`, worker, role, timeout_seconds, raw/signed exit code, elapsed_ms, prompt size, stdout/stderr末尾を保存した。

## Q&A / timeoutの所見

- 以前の120秒はWorkerの120秒設定ではなく、外側Harnessが先に終了した事象。Codex Q&A単体は約12〜17秒で応答した。
- 今回のClaudeはWorker timeout 30秒で明示的に終了し、Codexへfallbackした。成功stdout中の文字列をresource limitとは判定していない。
- Codexが返した同一質問に対してControllerが同じReviewを再実行するため、回答がartifactへ反映されない場合に再質問ループが残る。これは次の修正対象として記録する。
- Q&Aのstep timing（保存・render）もJSONLに残る。SQLiteの質問状態を手編集してCOMPLETEにした証拠はない。

## 実装変更

- Roleごとの既定候補順 `DEFAULT_ROLE_PRIORITY` と `role_router_from_adapters()` を追加。
- adapterが特定Roleに存在しない場合、`list index out of range` ではなく構造化 `WORKER_ERROR` を返すよう修正。
- Worker invocationおよびQ&A stepの診断JSONLを保持。
- README / hikitsugi.mdに、Codex基準E2E、4AIの環境制約、未達を偽装しない判定を追記。

## 残課題の分類

- `MVP_REQUIRED_AND_FIXED`: checkpoint/rollback、dirty tree保護、files_changed実状態検証、CompleteGate freshness、Director directive、DOC_SYNC、Codex基準Main Graph。
- `INTENTIONAL_SIMPLIFICATION`: Impact Analysisを独立外部グラフにせずController内部で処理。README自動生成ではなく、Controller-owned status callbackを正本とする。
- `POST_MVP`: Claude/Grok/agyの全Roleでの安定運用、同一runの2AI COMPLETE、Q&A回答をReview再実行へ確実に反映するループ収束。

## 検証

```text
uv run pytest tests/test_worker.py -q  -> 47 passed
uv run pytest -q                       -> 実行結果をcommit直前に再確認
git diff --check                        -> commit直前に再確認
```

本結果は、受入未達と既知制限を含む状態をそのまま記録したものである。
