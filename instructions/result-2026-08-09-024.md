# Result: instruction-2026-08-09-017

判定: `V1_1_INIT_ACCEPTED`

## 実装内容

- 公開CLIに `init` サブコマンドを追加した。
- `--workspace`、`--db`、`--run`、`--request` を受け付ける。
- オプションは `init` の前後どちらに置いても利用できる。
- Git repository root、存在するdirectory、clean treeを検証する。
- 空request、重複run ID、非Git directory、存在しないworkspaceを拒否する。
- 検証失敗時はSQLiteを作成しない。
- SQLite schema version 4に `run_inputs` を追加した。
- runのworkspaceと初期要求を正式入力として保存する。
- runを `IDLE` で作成し、既存のTransitionLoggerで `START` を記録して `DESIGN` に遷移する。
- run開始診断をworkspace外のJSONLへbounded metadataとして記録する。request本文や秘密情報は記録しない。
- `status`、`questions`、`show`、`answer`、`answer-batch` の既存CLI互換性を維持した。
- README、hikitsugi.md、note.mdをv1.0.1の利用方法に同期した。

## CLI実動確認

fresh Git workspace `agent-controller-fresh-20260809-022` に対して、次を実行した。

```bash
uv run agent-controller init \
  --workspace C:\\project\\agent-controller-fresh-20260809-022 \
  --db C:\\project\\init-e2e.db \
  --run TODO001 \
  --request "Build a Python CLI TODO app"
```

結果:

- exit code: 0
- run: `TODO001`
- position: `DESIGN`
- SQLite input workspace: 正しい絶対パス
- SQLite input request: 正しい初期要求
- first transition: `START`
- `status`: DESIGNとCompleteGate blockerを表示

## テスト

専用テスト:

```text
5 passed in 3.79s
```

通常テスト:

```text
277 passed, 4 skipped in 48.69s
```

専用テストでは成功、dirty tree、非Git directory、空request、重複run ID、CLI入口、正式入力保存、診断ログを確認した。

## 既知の境界

`init` はrunを作成して `DESIGN` へ入る公開入口である。Workerの選択や外部AI実行設定は既存Controller経路の責務として分離している。`--resume` は今回追加せず、既存run操作との責務混同を避けた。

Claudeの長いReviewer payload timeoutというv1.0既知制限は変更していない。Codex fallbackとCompleteGateの設計も維持している。

## 最終確認

- package version: `1.0.1`
- `git diff --check`: PASS
- commit/push: 実施
- `HEAD == origin/main`: 確認
- working tree: clean
